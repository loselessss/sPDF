# sPDF 1.28.5 릴리스 노트

릴리스 날짜: 2026-09-03

## 1.28.0 주요 변경 - 2026-09-02

### 성능 개선

- Soft Light·Multiply·Screen·Overlay 등 11개 혼합 모드를 사용하는 격리된 PDF 그룹도 기존 배경과 그룹 불투명도를 유지하면서 GPU 경로로 표시합니다.

## 1.28.1 개선 - 2026-09-02

- 격리된 PDF 그룹의 색조(Hue)·채도(Saturation)·색상(Color)·명도(Luminosity) 혼합도 기존 배경과 그룹 불투명도를 유지하면서 GPU 효과로 처리합니다.

## 1.28.2 개선 - 2026-09-02

- 중첩된 도형·텍스트 클리핑 안의 PDF 혼합 그룹도 기존 배경과 잘린 경계를 유지하면서 GPU 경로로 표시합니다.

## 1.28.3 개선 - 2026-09-02

- 알파·밝기·이미지 마스크 안의 혼합과 클리핑도 GPU로 처리하며, 중첩 마스크를 지원합니다.
- 색상 밝기 마스크는 PDF 마스크 전용 색 변환을 적용하고, 회색조 마스크 이미지는 지정된 이미지 보간 설정을 유지합니다.
- 비격리·knockout 그룹과 마스크 보정 함수는 CPU 대체 경로를 유지합니다.

## 1.28.4 개선 - 2026-09-02

- 독립 실행 창의 문서 탭을 제목표시줄로 옮기고 `파일.pdf [읽기/GPU]`, `[편집/CPU]`처럼 표시합니다.
- CPU/GPU 표시를 합성이 아닌 실제 래스터화 기준으로 바꿨습니다. CPU 이미지는 CPU, 혼합 래스터화는 CPU+GPU로 표시합니다.

## 1.28.5 개선 - 2026-09-03

- GPU로 표시하는 페이지의 벡터와 글자 윤곽 가장자리가 부드럽게 보이도록 그레이스케일 안티앨리어싱을 명시적으로 유지합니다.
- GPU 장면 준비가 오래 걸리는 어려운 페이지는 짧게 기다린 뒤 CPU 표시 경로로 전환해 리더가 오래 멈추지 않게 했습니다.
- 일반적인 선형·방사형 PDF 그라데이션은 더 적은 그리기 작업으로 GPU 경로에 남습니다.

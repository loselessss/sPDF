# sPDF 1.23.0 릴리스 노트

릴리스 날짜: 2026-09-02

## 1.23.0 - 2026-09-02

### 새 기능

- 지원되는 Windows 환경에서 리더와 편집 창의 보이는 PDF 영역을 Direct2D로 표시합니다.
- 화면 렌더러 메뉴에서 자동, GPU(Direct2D), CPU(PyMuPDF)를 선택할 수 있습니다. 변경 후 sPDF를 다시 실행합니다.
- 문서 창 제목에 현재 사용 중인 `[GPU]` 또는 `[CPU]`가 표시됩니다. Direct2D 하드웨어를 사용할 수 없으면 GPU 선택이 비활성화됩니다.

### 성능 개선

- 페이지 미리보기·선명한 화면 영역·검색 결과·텍스트 선택·편집 가능 글자 테두리를 GPU로 합성합니다.
- 글자·도형·배치 이미지로 이루어진 일반적인 페이지는 원래 글자 모양과 위치를 유지하면서 GPU 경로로 직접 표시해 확대·이동 때 더 빠르게 다시 그립니다.
- 탭을 숨기거나 닫으면 GPU 자원을 해제하며, 상세 이미지 캐시는 탭당 64 MiB로 제한합니다.

### 개선

- GPU 화면 표시를 시작할 수 없거나 사용 중 문제가 생기면 문서를 그대로 유지하고 기존 표시 방식으로 전환합니다.
- 복잡한 클리핑·그라데이션·마스크·투명도 그룹·선 모양이 있는 페이지는 표시 품질을 유지하도록 자동으로 전체 PyMuPDF 경로를 사용합니다.
- 편집 모드에서 페이지 끝을 넘어 휠을 돌려도 이전·다음 페이지로 자동 이동하지 않습니다.

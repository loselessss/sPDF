# PDF 편집기 설계 계획서 (v1)

작성일: 2026-07-15 · 위치: `C:\PDF Editor`

이 문서의 초기 설계는 이력으로 보존합니다. 현재 구현 상태는 README를 기준으로 하며,
2026-08-31 조판 기능 확장 초안은 [조판·레이아웃 편집 업데이트 계획](docs/LAYOUT_EDITING_PLAN.md)에 정리했습니다.

## Studio 개발 제1원칙 — 한 파일은 한 작업 모드만 소유한다

앞으로의 제품 방향은 **sPDF Studio**이며, 리더와 편집·조판은 **서로 다른 OS 프로세스**로 실행한다. 다만 같은 파일을 두 모드가 계속 동시에 소유하지 않는다. 대상 프로세스가 문서를 정상적으로 연 것을 확인한 뒤 원래 탭을 닫고, 마지막 탭이면 원래 창과 프로세스도 종료한다.

- GPU 렌더러 코드는 공통으로 사용하더라도 문서 상태·이벤트 루프·GPU 컨텍스트·캐시는 프로세스별로 소유한다. 모드 인계 중 원래 창은 대상의 열기 성공을 비동기로 기다리며 UI를 막지 않는다.
- 시작 모드는 설정에서 리더 우선 / 편집 우선으로 선택하는 계획이며, 필요할 때 해당 프로세스를 연다. 읽기 기능에 조판 모듈을 미리 올리지 않는다.
- 저장은 임시 파일에 완성한 뒤 안전하게 교체한다. 인계 전 열기 실패·시간 초과에는 원래 탭을 유지하고, 인계 후 편집기 오류에는 디스크의 마지막 정상 문서와 독립 복구 사본을 이용해 리더를 다시 열 수 있게 한다.
- 일반적인 2D PDF·조판 작업을 기준으로 삼고, 극단적인 메모리 부족을 설계의 주된 전제로 삼지는 않는다. 다만 캐시 상한·자원 회수는 유지하며 시스템 전체 자원 고갈까지 완전히 격리한다고 보장하지 않는다. 3D·전지 크기 조판의 언급은 고부하 예시이며 새 기능 요청으로 추가하지 않는다.
- **초안 작성 당시(1.19.2)** 리더·편집 창은 같은 프로세스였다. 2026-08-31 별도 구현 지시에 따라 sPDF 1.20.0에서 OS 프로세스 분리·시작 설정·안전 저장·독립 복구를 구현했다. 제품 이름은 sPDF이며 GPU 렌더러 교체와 조판은 후속 과제다. [구현과 검증 범위](docs/PROCESS_ISOLATION.md)를 참고한다.

아래의 초기 설계 항목은 이력이며, Studio 확장 시에는 이 제1원칙을 우선한다.

### 조판 개발의 선행 조건 — 리더·편집기 모두 GPU 렌더링

프로세스 격리를 유지한 채 **GPU 렌더링 기반을 먼저 검증하고 조판 편집을 확장한다.** 확대·축소·화면 이동·개체 드래그·크기 조절·회전의 반응성을 우선한다. 현재의 CPU 래스터 이미지 확대·GPU 합성만으로 완료 처리하지 않고, PDF 벡터·글자와 조판 개체를 GPU로 그리는 경로를 검증한다.

공통 렌더러 코드는 각 프로세스에서 독립 실행한다. 문자 흐름·정렬·저장 등 CPU 작업은 변경 부분만 계산하고 긴 작업으로 UI를 막지 않도록 별도 최적화한다. 동일 문서·장비에서 지연과 화면 품질을 비교하며, GPU 사용 자체를 편집 속도 향상의 증거로 삼지 않는다. 1.22.0에서 이미지 합성 기반의 1단계를 적용했고 1.23.0에서 단순 벡터 경로, 원래 glyph 윤곽, 배치 이미지 장면의 Direct2D 표시를 시작했다. 1.24.0에서는 중첩된 벡터 경로 클리핑과 색상 스텐실 이미지 마스크를 연결했고, 1.25.0에서는 텍스트 클리핑·제한된 shading 이미지·일반 격리 투명도 그룹을 추가했다. 1.26.0에서는 소프트/이미지 클리핑 마스크와 복잡한 선 스타일을 Direct2D로 옮기고 페이지별 렌더링 진단을 추가했다. 특수 혼합 모드·비격리 반투명 그룹과 조판 개체의 전체 GPU 경로 검증은 계획으로 유지한다.

**확정된 사항**: UI 한국어 고정 · 탐색기 연동은
레지스트리 등록 방식(§8) · OCR은 RapidOCR 기본 + Claude API 옵션(§3.3)

## 1. 목표

Windows 데스크톱 PDF 뷰어·편집기를 기반으로, 정밀한 개체 배치와 다중 페이지 조판 도구로 확장한다. 조판은 개발 예정이며 현재 제공 기능과 구분한다.

아래는 초기 개발의 핵심 4기능이며, 이후 확장 계획은 §8과 상세 조판 계획을 따른다.

1. **페이지 미리보기** — 썸네일 사이드바 + 메인 뷰(줌/스크롤)
2. **OCR** — 스캔본 PDF에 텍스트 레이어를 입혀 "검색/복사 가능한 PDF"로 변환
3. **복사·붙여넣기** — 마우스 드래그로 텍스트 선택 → Ctrl+C
4. **내용 편집(텍스트만)** — 기존 글자를 클릭해서 고치기

## 2. 기술 스택

| 역할 | 선택 | 이유 |
|---|---|---|
| GUI | **PyQt5** | BandWagon과 동일 — 코드 패턴/노하우 재사용 |
| PDF 엔진 | **PyMuPDF (fitz)** | 렌더링·텍스트 추출·편집·저장을 하나로 해결. pip 설치만으로 끝 |
| OCR (기본) | **RapidOCR** (PaddleOCR 모델, ONNX) | pip 설치만으로 끝(별도 설치 프로그램 불필요), 한글 인식 품질이 Tesseract보다 좋음 |
| OCR (옵션1) | **PaddleOCR-VL** (로컬 LLM, 약 1B) | 어려운 페이지용. 오프라인·무료, 단 모델 수 GB + CPU에선 느림 |
| OCR (옵션2) | **Claude API** (vision) | 손글씨 등 최후 수단. API 키 필요, 페이지당 과금 |

의존성: `pip install PyQt5 PyMuPDF rapidocr-onnxruntime` (+ 옵션: `anthropic`). 현재 PC에 모두 미설치 — 첫 구현 때 같이 설치.
만약 Python 3.14에서 onnxruntime 휠이 아직 없으면 Tesseract로 폴백(§6-1의 원안).

## 3. 기능 설계

### 3.1 페이지 미리보기
- 왼쪽: 썸네일 리스트(레이지 렌더링 — 보이는 것만 렌더, 메모리 절약)
- 오른쪽: 메인 페이지 뷰. 줌(Ctrl+휠), 페이지 이동(PgUp/PgDn, 썸네일 클릭)
- 렌더 캐시는 현재 페이지 ± 2페이지만 유지 (대용량 PDF에서 RAM 폭주 방지)

### 3.2 텍스트 선택·복사
- PyMuPDF의 단어 단위 좌표(`page.get_text("words")`)로 드래그 영역과 교차하는 단어를 하이라이트
- Ctrl+C → 클립보드. 스캔본(텍스트 레이어 없음)은 OCR 후에 가능해짐
- "붙여넣기"는 §3.4 편집 모드의 텍스트 입력으로 처리 (PDF에 자유 붙여넣기는 곧 텍스트 삽입)

### 3.3 OCR — 2단계 구조
엔진은 어댑터로 추상화해서 교체/추가 가능하게 설계.

**1단계 — 로컬 (RapidOCR, 기본)**
- 페이지 단위 또는 전체 문서 실행 (백그라운드 스레드, 진행바 표시)
- 단어별 좌표(바운딩 박스)를 얻어 **보이지 않는 텍스트 레이어**로 삽입
  → 원본 외관 그대로, 검색/복사 가능해짐
- 언어: 한국어+영어 동시 인식. 오프라인·무료

**2단계 — 로컬 AI LLM (PaddleOCR-VL, 옵션)**
- 소형(약 1B 파라미터) 문서 특화 vision-LLM. 한국어 포함 109개 언어,
  표/수식/복잡한 레이아웃에 강하고 **좌표도 주므로** 텍스트 레이어 삽입에
  그대로 사용 가능. 오프라인·무료
- 비용: 모델 다운로드 수 GB + 실행 시 RAM 수 GB. GPU 없으면 페이지당
  수십 초 수준으로 느림 → RapidOCR 대체가 아니라 "어려운 페이지용 옵션"
- 설정에서 활성화 시에만 모델 다운로드 (미설치면 메뉴 비활성)

**3단계 — 클라우드 AI (Claude API, 옵션)**
- 손글씨 등 로컬 AI로도 안 되는 최후 수단. vision 모델은 좌표를 정밀하게
  못 주므로 로컬 OCR 좌표에 AI가 읽은 텍스트를 입혀 교정하거나,
  결과를 사이드 패널에 띄워 복사용으로 제공
- 설정에 API 키 입력란. 키 없으면 메뉴 비활성
- 인터넷 필요, 페이지당 소액 과금 — 실행 전 페이지 수 확인 대화상자

### 3.4 텍스트 편집 — 방식과 한계 (중요)
PDF는 워드가 아니라 "글자를 좌표에 찍어놓은" 포맷이라 진짜 인라인 편집은 없음. 현실적인 방식:

- 편집 모드에서 텍스트 블록 클릭 → 해당 줄의 글자를 지우고(redaction) 같은 자리에 새 텍스트를 다시 씀
- **폰트**: 원본 폰트 재사용이 안 되는 경우가 많아 대체 폰트(맑은 고딕 등)로 다시 씀 → 모양이 미묘하게 달라질 수 있음. 크기·색은 원본에서 읽어와 맞춤
- **리플로우 없음**: 글자가 길어져도 다음 줄로 안 밀림. 해당 줄 안에서만 교체 (넘치면 자간/크기 자동 축소 또는 경고)

**스캔본 편집** (지원함):
- OCR을 먼저 실행해 단어 좌표를 확보한 뒤, 편집할 단어 클릭 →
  ① 그 영역을 **주변에서 샘플링한 배경색으로 칠해** 원본 글자를 지우고
  ② 같은 자리에 새 텍스트를 실제 텍스트 객체로 그림(검색도 됨)
- 배경이 균일한 문서는 티가 거의 안 나고, 노이즈 많은 스캔은 지운 자리가
  약간 표시날 수 있음 (필요시 cv2 inpaint로 개선 여지)
- OCR 없이도 임의 위치에 "텍스트 박스"(배경 사각형 + 새 글자)를 얹는
  자유 편집 모드 제공

### 3.5 추가 제안 (요청하신 "또 필요한 게 있을까")
반쯤 공짜로 얻을 수 있어서 넣기를 권장:
- **페이지 조작**: 회전 / 삭제 / 순서 변경(썸네일 드래그) / 다른 PDF 병합 / 추출(분할)
- **텍스트 검색** (Ctrl+F) — 복사 기능과 같은 텍스트 레이어를 쓰므로 거의 공짜
- **다른 이름으로 저장** + 저장 전 원본 자동 백업
- **최근 파일 메뉴**, 드래그&드롭으로 열기

보류 권장 (필요해지면 그때):
- 주석(하이라이트/메모), 폼 채우기, 전자서명, 이미지 삽입, 암호 설정

## 4. 아키텍처

BandWagon과 같은 "메인윈도우 + 믹스인 + Qt 비의존 코어" 구조:

```
pdfeditor/
  app.py        # MainWindow — 믹스인 조립
  viewer.py     # ViewerMixin — 썸네일/메인뷰/줌/페이지 이동
  textsel.py    # TextSelectMixin — 선택/복사/검색
  editing.py    # EditMixin — 텍스트 편집 + undo/redo
  ocr.py        # OCR 워커(QThread) + 엔진 어댑터 (Qt 시그널만 의존)
  core.py       # PyMuPDF 래핑 — 열기/저장/렌더/텍스트추출 (Qt 비의존)
  widgets.py    # PageView, ThumbList
  i18n.py       # ko/en (BandWagon 방식 그대로)
run.py / run.pyw
```

- **undo/redo**: BandWagon처럼 "원본 + 연산 리스트 재생" 방식. 편집 연산(텍스트 교체, 페이지 삭제 등)을 기록하고 되돌리기 시 원본에서 재적용
- **저장**: 항상 새 파일로 쓰고 성공 시 교체 (원본 파손 방지). incremental save는 쓰지 않음

## 5. 개발 단계

| 단계 | 내용 | 검증 방법 |
|---|---|---|
| v0.1 | 뷰어 — 열기/썸네일/줌/페이지 이동 | 실제 PDF로 화면 확인 |
| v0.2 | 텍스트 선택·복사 + 검색 | 텍스트 PDF에서 복사 → 메모장 붙여넣기 |
| v0.3 | OCR + 검색가능 PDF 저장 | 스캔본에서 한글 복사 확인 |
| v0.4 | 텍스트 편집(일반 PDF) + undo/redo + 저장 | 편집 → 저장 → 다른 뷰어로 열어 확인 |
| v0.5 | 스캔본 편집(배경 지우기 + 덮어쓰기) | 스캔본에서 글자 교체 → 인쇄 미리보기 확인 |
| v0.6 | 페이지 조작(회전/삭제/순서/병합) | 화면 확인 |
| v0.7 | AI OCR 옵션(PaddleOCR-VL, Claude API) | 저화질 스캔으로 비교 |

## 6. 결정 사항 (확정)

1. **OCR 엔진**: RapidOCR 기본 + Claude API 옵션 (§3.3).
   당초 후보였던 Tesseract는 별도 설치 프로그램이 필요하고 한글 품질도
   RapidOCR보다 떨어져 제외. 단, Python 3.14용 onnxruntime 휠이 없으면
   Tesseract로 폴백.
2. **UI 언어**: 한국어 고정 (i18n 모듈 생략 — 문자열 직접 기입)
3. **배포**: 본인용. exe 빌드 없이 `run.pyw` + 탐색기 연동(§8)
4. **추가 기능**: §3.5 권장 목록 포함 (페이지 조작·검색·백업·최근 파일)

## 7. 리스크

- **텍스트 편집 품질**: 폰트 대체로 인한 외관 변화가 이 도구의 가장 큰 한계. "완벽 재현"이 아니라 "내용 수정"이 목표임을 전제
- **암호화 PDF**: 열 때 비밀번호 입력만 지원, 편집 제한 플래그는 존중
- **초대형 PDF**(수백 페이지 스캔본): 레이지 렌더링 + 캐시 제한으로 대응 (§3.1)

## 8. 개발 계획
 - [x] 리더 / 기존 편집기의 OS 프로세스 격리 — sPDF 1.20.0 (조판은 미구현)
   - 시작 모드 선택, 비동기 프로세스 간 통신, 안전 저장과 독립 복구
   - 1.22.0부터 같은 파일의 모드 전환은 대상 열기 성공 뒤 원래 탭을 닫고, 마지막 탭이면 원래 프로세스 종료. 실패·시간 초과에는 원래 창 유지
   - 공통 GPU 렌더러는 코드만 재사용하고 실행 상태와 자원은 프로세스별 소유
 - [x] 설치 진입점 분리 — sPDF 1.21.0, 하나의 설치 프로그램과 실행파일, 리더·편집기 바로가기 두 개
   - 시작 메뉴에 **sPDF 리더**와 **sPDF 편집기**를 모두 등록하고, 바탕화면 바로가기는 설치 시 선택하도록 구성
   - 같은 `sPDF.exe`를 각각 `--workspace reader` / `--workspace editor` 실행 인자로 시작하며, 환경변수로 모드를 구분하지 않음
   - 각 실행은 별도 OS 프로세스로 동작하고, 제품 이름·설치·업데이트는 하나의 sPDF로 유지
   - 명시적 실행 인자는 저장된 시작 설정보다 우선하며, 인자 없이 실행하면 기존 리더 우선 / 편집 우선 설정을 따름
   - sPDF가 PDF 연결 프로그램인 경우 더블클릭은 기본 리더 우선을 유지하되 시작 설정을 따름. 사용자 동의 없이 Windows 기본 연결을 변경하지 않음
   - 앱 안에서 반대 모드를 열 때 읽던 페이지·배율을 이어가는 기존 흐름 유지
   - 검증: 설치·업데이트·제거 시 두 바로가기의 생성·갱신·정리, 공백·한글 문서 경로 전달, 설정과 무관한 명시적 모드 실행, 실제 서로 다른 PID 확인
   - 시작 메뉴에는 두 역할을 항상 등록하고 바탕 화면 바로가기는 역할별 선택 항목으로 제공. 1.20 이하의 단일 바로가기는 업데이트 설치 때 정리
 - [ ] 조판 개발 선행 조건: 리더·편집기 GPU 렌더링 기반 검증
   - 1단계(1.22.0): 독립 실행 리더·편집기에 같은 512px 타일·OpenGL 이미지 합성 경로와 탭당 64 MiB 캐시 상한 적용. 편집 상호작용과 CPU 표시 대체 경로 유지
   - 2단계(1.23.0): 실제 Qt HWND에 ABI v4 D3D11/DXGI/Direct2D 표면 연결. PyMuPDF 512px 타일·회전·검색·선택·편집 테두리 합성, 캐시 연동 폐기, 숨김·닫기 자원 회수와 Qt 대체 경로 적용
   - Windows 목표 백엔드: D3D11 장치 + DXGI flip-model swap chain + Direct2D device context + DirectWrite. [설계·검증 기준](docs/WINDOWS_RENDERING_BACKEND.md)
   - 네이티브 ABI v9: 하드웨어 D3D11/Direct2D/DirectWrite 초기화, HWND swap chain 프레임·BGRA bitmap·행렬·반투명 overlay·immutable line/Bézier geometry·변환 geometry group·geometry realization·중첩 clip/opacity/mask layer·사용자 선 스타일·stroked clip geometry·resize·해제와 실제 앱 연결 완료
   - 3단계 시제품(1.23.0): MuPDF CPU 파서가 반환한 단순 fill/stroke 경로와 원래 글꼴 glyph 윤곽을 Direct2D geometry로 캐시해 GPU rasterize하고, CPU에서 디코딩한 배치 이미지는 원래 행렬로 GPU 확대·합성. 기존 글자를 DirectWrite로 대체하거나 재조판하지 않으며 페이지 이미지 장면은 64 MiB로 제한
   - Direct2D는 PDF 해석기가 아니므로 1차 타일 합성과 실제 PDF 벡터·글자 GPU 경로를 분리. 기존 PDF 글자를 DirectWrite로 임의 재조판하지 않음
   - 현재 CPU 래스터화+OpenGL 합성과 PDF 벡터·글자·조판 개체의 GPU 렌더링을 구분
   - 확대·이동·개체 드래그·변형에서 변경 부분만 갱신하고, 문자 재배치·저장 등 CPU 병목은 별도로 최적화
   - 4단계(1.24.0): MuPDF 표시 순서를 그대로 기록하고 벡터 경로 clip push/pop을 Direct2D 중첩 layer로 연결. 클리핑된 글자·도형·이미지도 지원 장면이면 GPU 경로 유지. 단색 이미지 마스크는 MuPDF가 해석한 알파와 원래 색을 premultiplied BGRA로 만들어 같은 이미지 상한 안에서 GPU 합성
   - 5단계(1.25.0): 원래 glyph 윤곽을 하나의 텍스트 clip geometry로 결합. 모든 shading 유형은 페이지 범위 안의 2배 해상도 premultiplied BGRA 이미지로 제한해 Direct2D 합성하고 64 MiB 장면 상한을 공유. 일반 혼합 모드의 격리 투명도 그룹은 중첩 opacity layer로 처리
   - 6단계(1.26.0): 알파/광도 소프트 마스크와 이미지 클리핑 마스크를 Direct2D command list·opacity mask로 처리. PDF 점선·선끝·선 연결·miter 설정을 Direct2D stroke style로 변환. 선택형 페이지 진단으로 GPU 직접/GPU 합성/CPU 대체와 원인을 표시
   - 7단계(1.27.0): 윤곽선 글자를 원래 glyph geometry로 GPU rasterize하고, 윤곽선 글자 클리핑과 stroked vector clip은 Direct2D widened geometry로 변환해 GPU layer mask로 적용
   - 다음 단계는 특수 혼합 모드·비격리 반투명 그룹·선을 이용한 클리핑과 드문 효과의 지원 범위를 넓히고, 페이지 전체 CPU 전환을 가능한 경우 명령/그룹 단위 대체로 축소하는 것. PDFium/Skia GPU canvas는 별도 비교 시제품 후보로 유지하고, 현재 PyMuPDF CPU Pixmap 경로와 품질·속도·배포 크기·호환성·라이선스를 비교하며 엔진 이름만으로 GPU 지원을 가정하지 않음
   - 동일 장비·문서에서 최대 800% 확대, 다중 개체 변형, 글꼴·투명도 품질, RAM/VRAM 상한과 CPU 대체 경로 확인
   - 하드웨어별 캐시 정책: 기본값은 `자동`, 사용자가 `낮음 / 보통 / 높음`으로 고정 가능. 리더·편집 프로세스가 각자 정책과 자원을 소유하며 다른 프로세스의 여유 메모리를 전제로 하지 않음
   - 설정의 화면 렌더러는 `자동(권장) / GPU / CPU`로 선택. 자동은 Direct2D 우선 후 안전 전환, GPU는 Direct2D를 명시적으로 시도하되 실패 시 문서를 유지하고 CPU로 전환, CPU는 호환성 진단용 PyMuPDF 경로. 렌더러 선택과 캐시 등급은 별도 설정으로 유지
     - 자동 판정은 GPU 이름이나 전용 VRAM 표기만 믿지 않고 `GlobalMemoryStatusEx`의 사용 가능 RAM과 DXGI `QueryVideoMemoryInfo`의 현재 budget/usage를 함께 사용. 내장 GPU의 공유 메모리, WARP, 원격 데스크톱도 구분
     - 낮음: CPU 상세 타일 32 MiB/탭, GPU bitmap 64 MiB/프로세스, 보이는 영역 우선·최대 24타일. 보통: 64 MiB/탭, 192 MiB/프로세스, 최대 48타일. 높음: 128 MiB/탭, 512 MiB/프로세스, 최대 96타일을 초기 후보값으로 두고 실제 측정으로 조정
     - 등급 상한과 별개로 사용 가능 RAM·DXGI budget의 일정 비율을 넘지 않으며 큰 단일 bitmap은 캐시하지 않음. 비활성·숨김 탭의 GPU bitmap은 즉시 해제하고 CPU 타일은 LRU로 회수
     - 장치 제거, 할당 실패, 시스템 메모리 압박이 감지되면 실행 중 한 단계씩 자동 하향하고 현재 화면에 필요한 타일은 품질을 낮추지 않고 보관. 자동 상향은 화면 이동 중 하지 않고 유휴 상태나 다음 실행에서만 적용
     - 등급은 화면 선명도 설정이 아니라 보존량·선행 준비량 설정이다. 낮음에서도 현재 보이는 영역의 최종 해상도와 편집 좌표는 같아야 하며, 차이는 재방문 시 다시 그리는 빈도와 메모리 사용량으로 제한
     - 검증 문서는 긴 논문, 고해상도 스캔, 투명도·그라데이션·복잡한 글꼴 PDF를 포함하고 첫 표시·800% 이동·재방문·탭 전환 지연, peak RAM/VRAM, cache hit율을 등급별로 기록
 - [ ] 조판·레이아웃 편집 확장 — [상세 초안](docs/LAYOUT_EDITING_PLAN.md)
   - 첫 배포에 mm 위치·크기, 캔버스·여백, guide/grid·스냅, TrimBox·bleed 영역 모델과 SVG 직접 배치 포함
   - 내용 크기 유지가 기본, 함께 맞춤은 별도 선택; 현재·선택·전체 페이지 적용과 실행 취소 지원
   - 정확한 개체 배치·정렬·간격·그룹과 기본 문자 서식을 우선 제공하는 안
   - 재편집 원본 작업 파일을 따로 저장하고, PDF 출력은 텍스트 유지(기본) / 글자 윤곽선 변환으로 구분; 출력해도 작업 원문·서식 보존
   - PDF 자체에는 표준 `PieceInfo`의 `/sPDF` private stream으로 작은 편집 정보를 선택 저장할 수 있게 검증. 다른 프로그램이 제거할 수 있으므로 `*.spdf-layout` 원본을 대체하지 않음
   - 2단계: 연결 텍스트 프레임, 문단·문자 스타일 관리, baseline grid, kerning/tracking
   - 3단계: 개체 스타일, 마스터 페이지·템플릿, 여러 페이지 공통 요소·번호·예외 관리, 페이지 전체 규칙의 검사/적용/강제
   - 개체별 Hue/Saturation/Colorize: 원본을 보존하는 색조·채도·단색화, 스타일 재사용·초기화·실행 취소; 이미지·텍스트·도형부터 검증 후 배치 PDF/SVG·그룹으로 확장
   - 컬러코드로 PNG·SVG 색상 맞추기: HEX 입력, 지정 색 그대로 적용 / 명암 유지 색 입히기 구분, PNG 투명도 보존·특정 색 교체, SVG 채움/선 선택·벡터 유지; 원본 비파괴·여러 개체 일괄 적용
   - 4단계: bleed/slug/crop marks 출력과 ICC 기반 CMYK·PDF/X 내보내기; 독립 규격 검증을 통과한 범위만 제공
   - 기존 정밀 배치·스타일·템플릿·출력 점검 항목에 중복 요청을 통합. EPS는 보류하고 PDF·이미지·SVG 중심으로 진행
   - 구현·버전·일정 미확정. 기존 PDF 전체 자유 편집을 보장하지 않으며, 읽기·편집 공통 GPU 렌더링은 별도 기술 검증과 연결
 - [x] 편집 상세 화면과 별도 페이지 구성 창 (1.19.0, 1.21.0 개선)
   - 읽기 창 리본 왼쪽에 큰 파란색 편집 모드 버튼, Ctrl+E로도 열기
   - 편집기는 상세 편집으로 시작하고, 현재 쪽 기본 크기를 하단 상태표시줄에 mm 단위로 표시
   - 페이지 구성 버튼·Ctrl+Shift+P로 별도 바둑판 창을 열어 한 쪽·여러 쪽 드래그 재정렬, PDF 삽입·삭제
   - 별도 페이지 구성 창과 상세 편집은 미저장 변경·실행 취소 이력을 공유하며 보이는 쪽과 주변만 썸네일 렌더링
   - 편집 전용 표시는 탭에만 유지하고 창 제목·별도 상단 모드 줄에서는 제거
   - 내장 모드는 기존 페이지 구성 대화상자와 편집·업데이트 정책 유지
 - [x] 독립 실행본 읽기 창 / 별도 편집 창 분리 (1.18.0)
   - 읽기 창에서 Ctrl+E로 독립 편집 창을 열고, 같은 실행 세션에서 저장 후 읽기 위치를 유지해 갱신
   - 내장 모드의 기존 read_only/주석/업데이트 정책과 별개로 동작
   - 편집 창에서 글자 크기·색상 조절, 기존 페이지 구성·주석·수동 OCR 제공
 - [x] 읽기 창 OpenGL 이미지 합성·부분 렌더링 (1.18.0)
   - 화면 크기의 QGraphicsView/OpenGL 표면, 불가 시 CPU 표시로 폴백
   - 기존 이미지 즉시 확대·마우스 위치 유지, 최대 800% 확대와 두 장 보기 지원
   - 현재 한두 쪽의 저해상도 미리보기와 보이는 512px 타일만 렌더링, 탭당 타일 캐시 64 MiB 상한
   - PDF 래스터화는 PyMuPDF CPU 작업이며 GUI 타이머로 한 타일씩 처리, 숨김·닫기 시 대기 작업 취소
   - 편집 창·내장 모드의 기존 표시 경로 및 수동 OCR·저장·실행 취소 흐름 유지
 - [x] 프레젠테이션 모드, 전체화면 모드 추가
 - [x] 페이지 회전 추가 및 상단 명령 모음 배치
 - [ ] 메인 페이지 내 즐겨찾기 위치 이동
 - [ ] PPT식 발표자 모드
   - 발표 화면과 발표자 화면을 별도 창으로 구성하고 듀얼 모니터를 지원
   - 발표자 화면에 현재 페이지, 다음 페이지 미리보기, 페이지 번호와 경과 시간 표시
   - 발표자 화면의 페이지 이동을 실제 발표 화면과 동기화하고 모니터 선택·교체 지원
   - 기존 F5 단순 프레젠테이션은 가벼운 단일 화면 모드로 계속 유지
 - [x] 다국어 UI 기반 구축 및 영어판 우선 제공
   - 코드에 직접 작성된 한국어 UI 문자열을 번역 리소스로 분리
   - 첫 다국어 배포에서는 영어 UI만 제공하고 내부 언어 코드는 확장 가능하게 구성
   - 메뉴, 대화상자, 상태 메시지, 도움말, 오류 문구의 번역 누락 검사 추가
   - PDF 내용과 OCR 언어 설정은 UI 표시 언어와 분리하여 기존 한국어·영어 OCR 유지

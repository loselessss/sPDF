# 변경 이력

[English](CHANGELOG.md) | 한국어

## 1.29.5 - 2026-09-05

### 성능 개선

- 마스크나 혼합 대상 전환을 가로지르지 않는 도형 클립은 임시 합성 표면 대신 가벼운 Direct2D layer로 처리합니다.
- retained Direct2D 장면이 선형·방사형 그라데이션 brush를 프레임마다 다시 만들지 않고 재사용합니다.
- 자동·GPU 강제 모드에서 단순 사각형 경로를 빠른 축 정렬 clip으로 처리합니다. 측정된 경계 차이는 기존 geometry clip 출력 대비 2/255 이내이며, 네이티브 렌더러 ABI는 v19입니다.
- 자동 모드는 빠른 전체 명령 검사로 매우 복잡한 쪽을 먼저 판별해 GPU 장면 추출을 기다리지 않고 CPU 화면을 표시한 뒤, 격리된 백그라운드 준비가 끝나면 캐시된 GPU 장면으로 자연스럽게 교체합니다.

### 버그 수정

- 미뤄진 문서 탭이 처음 화면에 나타날 때 백그라운드 GPU 준비를 시작해, 초기 CPU 표시 경로에 계속 머무르지 않게 했습니다.
- 렌더링 진단에 현재 CPU, GPU, CPU+GPU 또는 CPU 표시와 백그라운드 GPU 준비 중인 상태를 명확하게 표시합니다.

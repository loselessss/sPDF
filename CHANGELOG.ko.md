# 변경 이력

[English](CHANGELOG.md) | 한국어

## 1.28.5 - 2026-09-03

### 개선

- 독립 실행 읽기·편집 창의 탭을 창 버튼과 같은 제목표시줄로 옮겨 중복 제목 줄을 없앴습니다.
- 탭 표시를 `파일.pdf [읽기/GPU]`, `[편집/CPU]`처럼 간결하게 정리했습니다.
- Direct2D GPU 페이지는 도형 그레이스케일 안티앨리어싱을 명시적으로 유지하고, 진단용 GPU 렌더러 엔진 배지를 페이지 위에 작게 표시합니다.
- GPU 장면 준비가 페이지당 1초를 넘으면 리더를 오래 붙잡지 않고 CPU 타일 경로를 유지합니다.
- 선형 및 대응 가능한 방사형 PDF 그라데이션은 여러 작은 벡터 밴드 대신 Direct2D 그라데이션 primitive로 표시합니다.

### 버그 수정

- CPU/GPU 표시는 실제 페이지 래스터화를 따릅니다. CPU 이미지의 GPU 합성은 CPU, 일부 CPU 래스터화가 섞인 페이지는 CPU+GPU로 표시합니다.

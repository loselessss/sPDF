# 변경 이력

[English](CHANGELOG.md) | 한국어

## 1.23.0 - 2026-09-02

### 새 기능

- 지원되는 Windows 환경에서 독립 실행 리더·편집 창의 보이는 512픽셀 PDF 타일을 Direct2D 비트맵 캐시에 올립니다.
- 독립 실행 설정에서 화면 렌더러를 자동, GPU(Direct2D), CPU(PyMuPDF) 중 선택할 수 있습니다. 변경 사항은 sPDF를 다시 실행하면 적용됩니다.
- 문서 창 제목에 현재 실제 화면 처리 장치를 `[GPU]` 또는 `[CPU]`로 표시하며, Direct2D 하드웨어를 사용할 수 없으면 GPU 선택을 비활성화합니다.

### 성능 개선

- Direct3D 11, DXGI flip-model swap chain, Direct2D로 페이지 미리보기·상세 타일·검색 표시·텍스트 선택·편집 가능 글자 테두리를 GPU 합성합니다.
- 글자·도형·배치 이미지로 이루어진 일반적인 페이지를 GPU 경로에서 직접 표시해 확대·이동 때 더 빠르게 다시 그립니다.
- 기존 PDF 글자의 원래 모양과 위치를 유지합니다. 제한된 이미지 캐시 용량을 넘는 페이지는 자동으로 전체 CPU 대체 경로를 사용합니다.
- 숨기거나 닫은 탭은 네이티브 비트맵과 화면 자원을 해제하며, 상세 이미지 캐시는 기존처럼 탭당 64 MiB로 제한합니다.

### 개선

- 네이티브 초기화·화면 표시·장치 오류가 나면 문서와 조작 좌표를 유지한 채 기존 Qt 표시 경로로 전환합니다.
- 편집 모드에서 페이지 위·아래 끝에 도달한 뒤 휠을 계속 돌려도 이전·다음 페이지로 자동 이동하지 않습니다.

### 기타

- 복잡한 클리핑·그라데이션·마스크·투명도 그룹·선 모양이 있는 페이지는 표시 품질을 유지하도록 전체 PyMuPDF 경로를 사용합니다.

## 1.22.0 - 2026-09-01

### 새 기능

- 독립 실행 편집 창도 리더와 같은 OpenGL 이미지 합성·화면 영역 타일 표시 방식을 사용합니다.

### 성능 개선

- 편집 창에서 확대·이동할 때 기존 미리보기를 즉시 재사용한 뒤 보이는 512픽셀 타일만 선명하게 갱신합니다. 상세 이미지 캐시는 탭당 64 MiB로 제한합니다.
- OpenGL을 사용할 수 없는 환경에서도 같은 편집 좌표와 저장 결과를 유지하는 CPU 표시 경로로 동작합니다.

### 개선

- 편집 모드로 문서를 정상 인계하면 같은 파일의 리더 탭을 닫고, 마지막 탭이면 리더 프로세스도 종료합니다. 열기 실패·시간 초과 때는 기존 리더를 유지합니다.
- 편집기에서 읽기 창으로 전환할 때도 미저장 확인 후 문서를 인계하고 원래 편집 탭을 닫아 같은 파일의 작업 모드가 계속 겹치지 않게 했습니다.
